1. Go to the traverse folder 
2. double click compile file
3. double click run file to see the result

Description:

XML Metadata Manager  		           (Feb-March 2011) 

Developed Xml Metadata Manager which creates metadata file(s) for each package in a given location on disk. The metadata files constitute a virtual dependency graph. The metadata file is then parsed recursively  to determine indirect dependencies 
